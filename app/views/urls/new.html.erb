<h1>Shorten Your URL</h1>
<%= form_with(model: @url, local: true) do |form| %>
  <div>
    <%= form.label :original_url, "Enter your URL" %>
    <%= form.text_field :original_url %>
  </div>
  <div>
    <%= form.submit "Shorten" %>
  </div>
<% end %>